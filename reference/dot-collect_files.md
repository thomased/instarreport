# Expand paths into a list of readable sheet files

Expand paths into a list of readable sheet files

## Usage

``` r
.collect_files(where, pattern, recursive, ext)
```

## Value

A list with `path` (full paths) and `label` (paths relative to the
directory they were found in, for naming).
