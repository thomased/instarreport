# Build a corpus from a list of reports

Build a corpus from a list of reports

## Usage

``` r
new_instar_corpus(x)
```

## Arguments

- x:

  A list of `instar_report` objects.

## Value

An `instar_corpus`.
