# Every reserved item id, in the order they appear in the sheet

Rows the sheet carries that are not framework items: the usage note, the
framework version, and the paper's own details.
[`read_items()`](https://instar-statement.org/reference/read_items.md)
peels all of them off before returning items.

## Usage

``` r
.RESERVED_FIELDS
```
