% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/items.R
\name{instar_set}
\alias{instar_set}
\title{Record what a study reports for one or more items}
\usage{
instar_set(items, ...)
}
\arguments{
\item{items}{An items table, typically from \code{\link[=instar_template]{instar_template()}} or
\code{\link[=read_items]{read_items()}}.}

\item{...}{Named values, one per \code{item_id}. Names must be valid item
ids; see \code{instar_items$item_id}. Values are length-one character
(or coercible to it), \code{NA}, or \code{NULL}.}
}
\value{
The updated items table.
}
\description{
The main way to fill in items from a script. Pass \code{item_id = "what the study reports"} pairs; each sets both the item's \code{value} and its
\code{status} in one step.
}
\details{
Setting \code{value} directly does not work, and fails quietly: \code{status} is
the single source of truth, and \code{\link[=instar_report]{instar_report()}} blanks \code{value}
wherever status is not \code{"reported"}. A table built by assigning
\code{value} alone renders as wholly unreported, with no error. This
function exists so that is not a trap you can fall into.

The three states follow the same convention as the \code{report} column of
the CSV sheet:\tabular{ll}{
   Value passed \tab Resulting status \cr
   a string \tab \code{"reported"} \cr
   \code{NA} \tab \code{"not_applicable"} \cr
   \code{""} or \code{NULL} \tab \code{"not_reported"} \cr
}
}
\examples{
items <- instar_set(
  instar_template(),
  subjects_taxon  = "Bombus terrestris (worker female); morphology + COI",
  subjects_n      = "n = 80; 72 analysed (10\% attrition)",
  env_field       = NA,          # laboratory study, does not apply
  proc_biosecurity = ""          # explicitly leave unreported
)
summary(instar_report(items, paper = list(title = "T", authors = "A")))

}
\seealso{
\code{\link[=instar_fill]{instar_fill()}} for the interactive equivalent.
}
