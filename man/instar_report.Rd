% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instar_report.R
\name{instar_report}
\alias{instar_report}
\title{Build an invertebrate welfare report}
\usage{
instar_report(
  items,
  paper = NULL,
  value_wrap = 75,
  unknown = c("error", "drop")
)
}
\arguments{
\item{items}{A data frame with at least \code{item_id} and a \code{report} (or
\code{value}) column, optionally a \code{status} column. Use
\code{\link[=instar_template]{instar_template()}} for an in-session template, \code{\link[=write_template]{write_template()}}
for a fillable CSV, and \code{\link[=instar_set]{instar_set()}} with \code{NA} to mark items
that do not apply.}

\item{paper}{A named list of paper metadata. Required: \code{title},
\code{authors}. Optional: \code{journal}, \code{version}, \code{doi}. May be omitted if
\code{items} came from \code{\link[=read_items]{read_items()}} on a file carrying the reserved
metadata rows, in which case those details are used.}

\item{value_wrap}{Integer; approximate characters per line for the
item text when the report is drawn. Defaults to \code{75}. Set here it
becomes the report's default, so the preview and the saved file
agree; override it per call in \link[=plot.instar_report]{plot()},
\link[=autoplot.instar_report]{autoplot()} or \code{\link[=save_figure]{save_figure()}} when tuning.}

\item{unknown}{What to do with \code{item_id}s in \code{items} that are not in
the framework. \code{"error"} (the default) stops; \code{"drop"} warns and
ignores them. Passed to \code{\link[=validate_items]{validate_items()}}.}
}
\value{
An object of class \code{instar_report}: a list with elements
\code{paper}, \code{items} (one row per framework item, with \code{value} and
\code{status}), \code{coverage}, \code{version} (the framework version the items
declared, or \code{NA}), and \code{value_wrap}.
}
\description{
Joins a study's filled-in items against the canonical \link{instar_items}
framework and returns a report object. The result is \emph{data}, not a
plot: it carries the paper metadata, the resolved item table, and a
coverage summary. Call \link[=plot.instar_report]{plot()} to draw the
standardised figure, \code{\link[=save_figure]{save_figure()}} to write that figure to disk, or
\code{\link[=write_report]{write_report()}} to write the deposit-ready CSV. If you want the
figure as an object to modify further, use
\link[=autoplot.instar_report]{autoplot()}.
}
\examples{
tmpl <- instar_set(
  instar_template(),
  subjects_taxon   = "Bombus terrestris (worker female); morphology + COI",
  proc_anaesthesia = NA
)

rep <- instar_report(
  tmpl,
  paper = list(title = "Demo", authors = "Smith et al. (2026)")
)
rep
summary(rep)

}
