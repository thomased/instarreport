% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_instar.R
\name{read_instar}
\alias{read_instar}
\title{Read INSTAR sheets from disk}
\usage{
read_instar(
  where,
  pattern = NULL,
  recursive = FALSE,
  subdir_names = FALSE,
  ext = c("csv", "xlsx"),
  quiet = FALSE,
  unknown = c("drop", "error")
)
}
\arguments{
\item{where}{A file path, a directory, or a character vector of
either. Directories are searched for readable sheets.}

\item{pattern}{Optional regular expression to filter file names within
directories. Applied after the extension filter.}

\item{recursive}{Logical; search directories recursively. Defaults to
\code{FALSE}.}

\item{subdir_names}{Logical; when recursing, prefix each sheet's name
with its directory path relative to \code{where}. Useful when a corpus is
organised into folders that mean something (by journal, by year), as
the prefix then survives into \code{\link[=instar_audit]{instar_audit()}} as a grouping
variable. Defaults to \code{FALSE}.}

\item{ext}{File extensions to accept. Defaults to \code{c("csv", "xlsx")}.
Reading \code{.xlsx} requires the \pkg{readxl} package.}

\item{quiet}{Logical; suppress the progress messages. Defaults to
\code{FALSE}.}

\item{unknown}{Passed to \code{\link[=instar_report]{instar_report()}}. Defaults to \code{"drop"}, so
that a sheet carrying an unrecognised \code{item_id} (a hand-edited row,
a later framework version) warns rather than failing the whole
corpus.}
}
\value{
An object of class \code{instar_corpus}: a named list of
\code{instar_report} objects, with attributes \code{failed} (a data frame of
unreadable files and their error messages) and \code{path} (what was
asked for).
}
\description{
Reads one or many completed INSTAR sheets and returns them as a single
\code{instar_corpus}. Point it at a file, a directory, or any mix of both:
the format is worked out from the file extension, and the result has
the same class whether it found one sheet or two hundred.
}
\details{
A file that cannot be read does not stop the run. It is reported in a
warning, left out of the corpus, and recorded with its error message
in the \code{failed} attribute, so a single corrupt supplement in a large
corpus is a thing to go and look at rather than a dead end.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{corpus <- read_instar("supplements/")
attr(corpus, "failed")     # what could not be read, and why
}\if{html}{\out{</div>}}
}
\section{Naming}{
Sheets are named by their DOI where the reserved rows carry one, and
by file name otherwise. Duplicate DOIs are warned about but kept: the
same study reaching a corpus twice through different routes will
otherwise inflate coverage estimates silently.
}

\section{Framework versions}{
Each sheet declares the framework version it was completed against.
A corpus mixing versions is warned about, because coverage is not
comparable across them: an item absent from half the sheets because it
did not exist yet is not the same as an item those studies failed to
report. Sheets written before versioning declare nothing and read as
\code{NA}.
}

\examples{
\dontrun{
# A directory of deposited supplements
corpus <- read_instar("supplements/")

# Organised by journal, with the folder kept as a grouping variable
corpus <- read_instar("corpus/", recursive = TRUE, subdir_names = TRUE)

# A single sheet, still a corpus of one
corpus <- read_instar("INSTAR.csv")
}

}
\seealso{
\code{\link[=read_items]{read_items()}} for a single sheet as an items table,
\code{\link[=instar_audit]{instar_audit()}} for coverage across a corpus.
}
