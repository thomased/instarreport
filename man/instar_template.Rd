% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/items.R
\name{instar_template}
\alias{instar_template}
\title{Return an empty items template for a study}
\usage{
instar_template(study_type = c("both", "lab", "field"))
}
\arguments{
\item{study_type}{Optional. One of \code{"lab"}, \code{"field"}, or \code{"both"}.
If supplied, items flagged as not applicable in that context are
pre-marked with \code{status = "not_applicable"}.}
}
\value{
An \code{instar_items} table with columns \code{item_id}, \code{item},
\code{domain}, \code{description}, \code{value}, \code{status}.
}
\description{
Convenience helper that returns a table with one row per framework
item, an empty \code{value} column for the user to fill in, and a \code{status}
column recording each item's state.
}
\details{
Write substantive content into \code{value} to report an item. Leave it
blank for items the study does not report. Use \code{\link[=instar_na]{instar_na()}} to mark
items that do not apply.
}
\examples{
tmpl <- instar_template("lab")
head(tmpl)

}
