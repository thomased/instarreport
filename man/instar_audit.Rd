% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{instar_audit}
\alias{instar_audit}
\title{Audit reporting coverage across many studies}
\usage{
instar_audit(x, ...)
}
\arguments{
\item{x}{What to audit. Any of:
\itemize{
\item a path to a directory of sheets, or a vector of file paths, which
are read with \code{\link[=read_instar]{read_instar()}};
\item an \code{instar_corpus};
\item a list of \code{instar_report} objects, or of items tables;
\item a data frame of scores in wide form, passed to
\code{\link[=audit_from_matrix]{audit_from_matrix()}}.
}}

\item{...}{Passed to \code{\link[=read_instar]{read_instar()}} when \code{x} is a path, or to
\code{\link[=audit_from_matrix]{audit_from_matrix()}} when \code{x} is a data frame.}
}
\value{
An object of class \code{instar_audit}: a list with
\itemize{
\item \code{items}, one row per framework item with counts and the percentage
of applicable studies that reported it;
\item \code{studies}, one row per study with its own coverage (as
\code{\link[=summary.instar_corpus]{summary.instar_corpus()}} returns);
\item \code{long}, one row per study-item pair, the tidy form to compute on;
\item \code{n}, the number of studies.
}
}
\description{
Summarises how consistently a set of studies reported each framework
item. This is the corpus-level view: \code{\link[=instar_report]{instar_report()}} describes one
study, \code{instar_audit()} describes a literature.
}
\examples{
\dontrun{
# Prospective: a folder of deposited sheets
audit <- instar_audit("supplements/")
audit
summary(audit)
plot(audit)

# Which items does the literature handle worst?
head(summary(audit)[order(summary(audit)$percent_reported), ])
}

}
\seealso{
\code{\link[=read_instar]{read_instar()}} to build a corpus, \code{\link[=audit_from_matrix]{audit_from_matrix()}} for
retrospective scoring.
}
