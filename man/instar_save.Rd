% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instar_report.R
\name{instar_save}
\alias{instar_save}
\title{Save a report figure to disk}
\usage{
instar_save(report, filename, width = 8.5, height = NULL, dpi = 300, ...)
}
\arguments{
\item{report}{An object of class \code{instar_report}.}

\item{filename}{Output file path (extension determines format; .pdf or
.png are recommended).}

\item{width}{Page width in inches. Defaults to 8.5".}

\item{height}{Page height in inches. If \code{NULL} (the default), a
compact height is chosen from the content, clamped to \verb{[6, 11]}.}

\item{dpi}{Resolution for raster formats. Defaults to 300.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The filename, invisibly.
}
\description{
Renders a report and writes it via \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. The default
page height is computed from the figure's natural content size, so the
saved file is as compact as the content allows with no large blank
regions. Pass an explicit \code{height} to override.
}
\examples{
\dontrun{
rep <- instar_report(list(title = "T", authors = "A"), instar_template())
instar_save(rep, "welfare_reporting.pdf")
}

}
