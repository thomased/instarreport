% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instar_report.R
\name{save_figure}
\alias{save_figure}
\title{Save a report's figure to disk}
\usage{
save_figure(
  report,
  path,
  width = 8.5,
  height = NULL,
  dpi = 300,
  value_wrap = NULL,
  ...
)
}
\arguments{
\item{report}{An object of class \code{instar_report}.}

\item{path}{Output file path. The extension chooses the format;
\code{.pdf} or \code{.png} are recommended.}

\item{width}{Page width in inches. Defaults to 8.5".}

\item{height}{Page height in inches. If \code{NULL} (the default), a
compact height is chosen from the content, clamped to \verb{[6, 11]}.}

\item{dpi}{Resolution for raster formats. Defaults to 300.}

\item{value_wrap}{Approximate characters per line for the item text.
Defaults to the value stored on the report.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The path, invisibly.
}
\description{
Renders a report's figure and writes it via \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. For
the report as a depositable CSV instead, use \code{\link[=write_report]{write_report()}}.
}
\details{
The default page height is computed from the figure's natural content
size, so the saved file is as compact as the content allows with no
large blank regions. Pass an explicit \code{height} to override.
}
\examples{
\dontrun{
rep <- instar_report(instar_template(), list(title = "T", authors = "A"))
save_figure(rep, "welfare_reporting.pdf")
}

}
