% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{as.data.frame.instar_corpus}
\alias{as.data.frame.instar_corpus}
\title{Coerce a corpus to a plain data frame}
\usage{
\method{as.data.frame}{instar_corpus}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An \code{instar_corpus}.}

\item{row.names}{Unused, for consistency with the generic.}

\item{optional}{Unused, for consistency with the generic.}

\item{...}{Unused.}
}
\value{
A data frame with columns \code{study}, \code{title}, \code{doi}, \code{journal},
\code{item_id}, \code{item}, \code{domain}, \code{group}, \code{status}, and \code{value}.
}
\description{
One row per study per framework item, in long form, carrying what each
study reported. This is the shape to compute on when the question is
about content rather than coverage, such as pulling every housing
description out of a corpus.
}
\details{
\link[=summary.instar_corpus]{summary()} gives one row per study, and
\code{\link[=instar_audit]{instar_audit()}} gives one row per item. This gives both at once.
}
\examples{
\dontrun{
d <- as.data.frame(read_instar("supplements/"))

# every housing description in the corpus
subset(d, item_id == "env_housing" & status == "reported",
       select = c(study, value))
}

}
