% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/items.R
\name{new_instar_items}
\alias{new_instar_items}
\title{Construct an items table}
\usage{
new_instar_items(x)
}
\arguments{
\item{x}{A data frame with at least \code{item_id} and \code{value} columns.}
}
\value{
\code{x} with a canonical \code{status} column and the \code{instar_items}
class prepended.
}
\description{
Adds the \code{instar_items} class to a data frame of reporting items and
guarantees the canonical shape: a character \code{value} column carrying
only substantive content (\code{NA} otherwise), and a \code{status} factor with
levels \code{"reported"}, \code{"not_reported"}, \code{"not_applicable"}.
}
\details{
If \code{x} has no \code{status} column, one is derived from \code{value} via
\code{\link[=.derive_status]{.derive_status()}}, which honours \code{"NA"} / \code{"N/A"} strings as an input
shorthand for not-applicable. Once inside an \code{instar_items} table,
\code{status} is the single source of truth.
}
\keyword{internal}
